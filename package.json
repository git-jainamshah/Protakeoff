{
  "name": "protakeoff-studio",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently --names \"API,WEB\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev-frontend": "npm run dev --prefix frontend",
    "dev-backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:studio": "npm run prisma:studio --prefix backend",
    "seed": "cd backend && DATABASE_URL='file:./prisma/dev.db' npx tsx prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
